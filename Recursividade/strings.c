/*Implemente uma função recursiva que verifique se duas strings são iguais (retorne 1) ou não (retorne 0).
Se duas strings são vazias, elas são iguais;
Se todos os seus caracteres são iguais, elas são iguais.*/
#include<stdio.h>
int stringIguais(string str1, string str2){
    if (str1 == 0 && str2 == 0)
    {
        return 1;
    }
    return 0;
}

int caracteresIguais(){

}
